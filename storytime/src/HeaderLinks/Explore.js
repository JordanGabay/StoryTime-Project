import React from "react";
import styled from "styled-components";

const Text = styled.p``;

const Explore = (props) => {
  return (
    <div>
      <Text>Hello</Text>
    </div>
  );
};

export default Explore;
