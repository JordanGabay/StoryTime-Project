import React from "react";
import styled from "styled-components";

const Text = styled.p``;

const Profile = (props) => {
  return (
    <div>
      <Text>Hello</Text>
    </div>
  );
};

export default Profile;
